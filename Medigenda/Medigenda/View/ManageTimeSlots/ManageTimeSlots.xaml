<Page
    x:Class="Medigenda.ManageTimeSlots"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Medigenda"
    xmlns:auto="using:AutoGenerateForm.Uwp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Icon="Save"     Label="Save Change"     Command="{Binding SaveButton}"/>
            <AppBarButton Icon="Add"     Label="Add WorkerSchedule"     Command="{Binding AddButton}"/>
            <AppBarButton Icon="Delete"  Label="Delete WorkerSchedule"  Command="{Binding DeleteButton}"/>
        </CommandBar>
    </Page.BottomAppBar>

    <!--
        Use of AutoGenerated Forms from attribut of a Class
        
        1.Call the uft using which contains AutoGenerator from the xmlns List Above this Code
        2.Add this.Datacontext = the ViewModelPage into the xaml.cs files -->

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="5*" />
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*" />
                <RowDefinition Height="10*" />
            </Grid.RowDefinitions>
            <ListView
                Grid.Row="1"
                ItemsSource="{Binding WorkerScheduleListing, Mode=TwoWay}" 
                SelectedItem="{Binding SelectedWorkerSchedule, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>

                        <TextBlock Text="{Binding Output, Mode=TwoWay}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel  Grid.Column="0">
                <TextBlock Text="Worker Schedule Management Page"/>
                <auto:AutoGenerator CurrentDataContext="{Binding SelectedWorkerSchedule, Mode=OneWay}"/>
                <TimePicker  Header="Beginning Hour"  Time="{Binding SelectedWorkerSchedule.Start_hour, Mode=TwoWay}"></TimePicker>
                <TimePicker  Header="End Hourt" Time="{Binding SelectedWorkerSchedule.End_hour, Mode=TwoWay}"></TimePicker>
            </StackPanel>
        </Grid>
    </Grid>
</Page>

