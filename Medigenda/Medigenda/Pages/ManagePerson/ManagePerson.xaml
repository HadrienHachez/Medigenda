<Page
    x:Class="Medigenda.ManagePerson"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Medigenda"
    xmlns:autogenerator="using:AutoGenerateForm.Uwp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Icon="Add" Label="Add Worker"/>
            <AppBarButton Icon="Delete"/>
        </CommandBar>
    </Page.BottomAppBar>
    
    <!--
        Use of AutoGenerated Forms from attribut of a Class
        
        1.Call the uft using which contains AutoGenerator from the xmlns List Above this Code
        2.Add this.Datacontext = this into the xaml.cs files
    -->
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="277*" />
            <ColumnDefinition Width="652*" />
            <ColumnDefinition Width="351*" />
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="31*" />
                <RowDefinition Height="300*" />
                <RowDefinition Height="29*" />
            </Grid.RowDefinitions>
            <ListView
                Grid.Row="1"
                Margin="0,0,0,7.326"
                ItemsSource="{Binding WorkerListing, Mode=TwoWay}"
                SelectedItem="{Binding SelectedStudent, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Last_name, Mode=TwoWay}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <autogenerator:AutoGenerator
            x:Name="form"
            Grid.Column="1"
            Margin="24"
            CurrentDataContext="{Binding SelectedStudent, Mode=TwoWay}" TitleForm="test" IsTitleEnabled="True"
            ValidationCollection="{Binding SelectedStudent.Validations, Mode=TwoWay}" />

        <autogenerator:ValidationSummary
            Grid.Column="2"
            AutoGenerator="{Binding ElementName=form}"
            ValidationCollection="{Binding SelectedStudent.Validations, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

    </Grid>
    

</Page>
